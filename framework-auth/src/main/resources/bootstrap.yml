spring:
  application:
    name: framework-auth
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: dev
        file-extension: yml
        shared-configs:
          - data-id: common-config.yml
            refresh: true

